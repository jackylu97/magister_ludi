node_modules
dist
.dream-loop/
.DS_Store
*.local

# The grid search's measurements (`scripts/gridSearch.ts`). A run's numbers are a
# reading, not a source: the table that matters is quoted in docs/bot-priorities.md.
gridsearch-results.json

# Claude Code agent worktrees (per-session scratch checkouts)
.claude/worktrees/

# The evolutionary tuner's run directories (`scripts/evolve.ts`): ledgers and
# champions are measurements, never sources — the winner is pasted into
# data/ai.json by hand and lands as a balance change.
.claude/scratch/

{
  "name": "magister-ludi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:slow": "TEST_TIER=slow vitest run",
    "test:all": "TEST_TIER=all vitest run",
    "test:sim": "vitest run test/sim",
    "test:mapgen": "vitest run test/mapgen",
    "test:render": "vitest run test/render",
    "test:ui": "vitest run test/ui",
    "test:stress": "TEST_TIER=all vitest run test/stress",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@types/three": "^0.185.4",
    "three": "^0.185.1"
  }
}

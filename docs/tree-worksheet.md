# Moved — `tree-worksheet.md`

Moved to `docs/deprecated/tree-worksheet.md` on 2026-09-11 (the docs
condensation). It was the user's tree canvas at revision 3, with the rationale
behind each node's name, move and merge; it is superseded by `docs/tech-tree.md`
(the as-built reference, whose Part 2 is generated from the rows) and by
`data/techs.json`, which carries the drawn lanes and columns as data.

Docblocks and tests across the tree cite this path — follow them to the new file.

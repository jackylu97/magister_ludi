# Moved — `orders-candidates.md`

Moved to `docs/deprecated/orders-candidates.md` on 2026-09-11 (the docs
condensation). It was the candidate-Orders list and the rarity proposal;
it is superseded by `docs/orders-and-doctrines.md` (the master list, sync-tested
against `data/statecraft.json`) and by `src/sim/statecraft/draft.ts`, which is
where the rarity draw and the skip's pity actually live.

Docblocks and tests across the tree cite this path — follow them to the new file.
